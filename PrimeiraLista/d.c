#include <stdio.h>

int main (){
	int a, b, c, d;//declarando as variáveis necessárias para o programa.

	scanf("%d %d %d %d", &a, &b, &c, &d);//entrada de valores

	printf("DIFERENCA = %d\n", (a*b-c*d));//saída de acordo com a especificação do problema

	return 0;	
}
